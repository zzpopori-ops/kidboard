# 호스트의 node_modules 는 macOS arm64 바이너리라 컨테이너에서 못 쓴다.
# 반드시 제외하고 컨테이너 안에서 npm ci 로 다시 깐다.
node_modules/
test-results/
playwright-report/
blob-report/
.DS_Store
.git/
Dockerfile
compose.yaml
.dockerignore
